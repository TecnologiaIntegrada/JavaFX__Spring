# FrontEnd conecta somente ao Backend (REST). Nunca ao PostgreSQL.
# Cadeia: JavaFX -> HTTP -> Spring Boot -> PostgreSQL (localhost:5432/projeto_j)
api.base-url=http://localhost:8080/api/v1
api.connect-timeout-seconds=10
api.request-timeout-seconds=30
